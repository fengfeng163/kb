- Create a new Class to store new functions
- Reference an existing Class using the extendsObject() method

![[Pasted image 20260818224053.png]]
Extending a Class means to add functionality (typically in the form of methods) to an existing Class without modifying the original script.

Create a New Script Include/Class, reference an existing Class using the extendsObject() method to include all its functionality and add script logic.

Commonly extended ServiceNow Classes:

- AbstractAjaxProcessor: makes AJAX calls from Client Scripts.
- LDAPUtils: used by LDAP integration to ServiceNow (for example, adding Managers to users, managing group membership, debug logging).
- Catalog*: set of Classes used for Service Catalog management (for example, UI building, Form processing).

### Extend an existing Class: extend AbstractAjaxProcessor
- Baseline AJAX processor Class
- Executes server-side, but is called client-side

Select the arrows or dots to navigate between the feedback loop examples.

![[Pasted image 20260818224218.png]]
![[Pasted image 20260818224231.png]]
![[Pasted image 20260818224244.png]]

AJAX – Asynchronous JavaScript and XML  Set of web development methods used client-side to send data to and receive data from the server.  

In this example, the alertGreeting() method is created. When it is called, it returns the string “Hello <name passed in from the client> ” to the calling script.  

For a list of available script includes, navigate to System Definition > Script Includes. You can personalize the list to show the Client callable column.

>Note: The base system does not include any ACL rules for client-callable script includes. Client-callable script include ACL rules honor the STAR (*) rule if they cannot find a more specific ACL rule for those resources.


